# sx glossary

## Worktree source branch

The existing local or remote Git branch selected or typed by the developer as the revision from which `sx` creates a worktree. It is not a request to create a new branch.
